---
description: Rebuild dist-package after any MCP changes so the distribution stays in sync
alwaysApply: true
---

# Rebuild Distribution After Changes

**REQUIRED:** After making any changes to MCP commands, tools, prompts, bootstrap, skills, or docs, you MUST rebuild `dist-package/` so the distribution reflects the latest code.

## Keep Documentation Up to Date

**REQUIRED:** Every code change must update the relevant documentation:

- **docs/implementation.md** — MCP tools inventory, conventions, field mapping, API reference
- **docs/setup-guide.md** — Command tables, setup steps
- **docs/testing-guide.md** — Tool quick reference, slash commands, troubleshooting
- **docs/tc-style-guide-and-consistency-strategy.md** — TC style and consistency rules

## Rebuild Command

```bash
npm run build:dist
```

This rebuilds `dist-package/` from the current source. No manual deploy step is needed — end users pull the latest install via the Vercel-hosted tarball, which is published through a separate Vercel workflow.

## When to Rebuild

- After adding or modifying MCP tools
- After changing the installer or bootstrap
- After updating setup-guide.md or conventions.config.json
- After any enhancement that affects the user-facing MCP
